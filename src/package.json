{
	"name": "build",
	"version": "0.0.0",
	"private": true,
	"scripts": {
		"build": "node-gyp rebuild && node -e \"require('addon-tools-raub/cpbin')('segfault')\""
	},
	"dependencies": {
		"addon-tools-raub": "5.2.x"
	}
}
